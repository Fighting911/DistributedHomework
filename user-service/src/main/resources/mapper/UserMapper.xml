<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seckill.user.mapper.UserMapper">

    <resultMap id="userResultMap" type="User">
        <id     property="id"        column="id"/>
        <result property="username"  column="username"/>
        <result property="password"  column="password"/>
        <result property="email"     column="email"/>
        <result property="phone"     column="phone"/>
        <result property="status"    column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, email, phone, status, created_at)
        VALUES (#{username}, #{password}, #{email}, #{phone}, 1, NOW())
    </insert>

    <select id="selectByUsername" resultMap="userResultMap">
        SELECT * FROM t_user WHERE username = #{username} LIMIT 1
    </select>

    <select id="selectById" resultMap="userResultMap">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

</mapper>
